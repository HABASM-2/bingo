# Generated from the packages actually installed in the project's `.venv`
# (there was no requirements.txt before this). `redis` is the one new
# package needed for the Bingo module - everything else already matches
# what's installed.
#
# Install with:  .venv\Scripts\pip install -r backend\requirements.txt

fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
alembic==1.18.5
pyjwt==2.13.0
python-telegram-bot==22.8
redis>=5.0.1
